{
  "name": "@portus-qc/camsnap",
  "version": "0.0.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./node-runtime": "./src/node-runtime.ts"
  },
  "dependencies": {
    "@portus-qc/contracts": "0.0.0",
    "@portus-qc/media": "0.0.0"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test test/*.test.mjs",
    "verify": "npm run typecheck && npm run test"
  },
  "license": "Apache-2.0",
  "description": "Camsnap local IP-camera reference adapter and Node runtime."
}
