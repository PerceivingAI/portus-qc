{
  "name": "@portus-qc/inspection-config",
  "version": "0.0.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./inspection.schema.json"
  },
  "dependencies": {
    "@portus-qc/contracts": "0.0.0"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test test/*.test.mjs",
    "verify": "npm run typecheck && npm run test"
  },
  "license": "Apache-2.0",
  "description": "Declarative prompt-defined inspection configuration with one selected vision capability."
}
